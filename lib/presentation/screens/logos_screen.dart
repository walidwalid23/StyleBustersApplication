import 'package:flutter/material.dart';

class LogosScreen extends StatelessWidget {
   LogosScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(
      child: Text("Logos Screen"),
    ),);
  }
}
